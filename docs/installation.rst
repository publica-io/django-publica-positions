============
Installation
============

At the command line::

    $ easy_install django-publica-positions

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-publica-positions
    $ pip install django-publica-positions
